# Architecture

The system is built on a distributed microservice-inspired architecture using Python (FastAPI/Pydantic) for the backend engines and React (Next.js/Tailwind) for the frontend workspace.
